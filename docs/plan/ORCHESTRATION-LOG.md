# Orchestration log — append-only

One line per orchestration event (agent launched/landed, verdicts, commits,
plan changes). Newest at the bottom. The overseer writes this; the point is
that any future session can reconstruct WHERE THE PROJECT IS from the repo
alone. The master plan lives in `v0.4-behind-view-plan.md`.

| When | Event |
|---|---|
| 2026-08-19 | v0.1 M0-M5 shipped via opencode fleet (PRs #1-#7, see docs/pr-log.md). |
| 2026-08-21 | v0.2+v0.3 shipped via PR #8 (pixel art, global input, shop strip, economy fix, active-app line). Opus review caught the global-path 3:1 mouse exploit pre-merge. |
| 2026-08-21 | macOS field test FAILED mechanics (no capture, dishonest moods, invisible store) → ADR 0010, Go-prototype mined for its proven design. |
| 2026-08-21 | User's design PDF ingested: behind-view + store modal + own/equip. v0.4 plan written (this dir). Standing rule recorded: overseer orchestrates only; subagents implement. |
| 2026-08-21 | W1-A (mechanics green-up, Sonnet) and W1-B (PDF→specs, Opus) launched in parallel with disjoint ownership. |
| 2026-08-21 | **ADR 0011: engine pivot.** The PDF design ships on its native stack (Go + HTML/NES.css) in `app/`; Bevy game frozen as legacy; opencode fleet retired from critical path. W1-B redirected mid-flight to spec for the web frontend. Plan Waves 2-3 rewritten. |
| 2026-08-21 | W1-A landed and committed: Rust legacy freeze, fully green. Verification tooling gap found: no pip/pypi access on this box — headless-browser plan may need npx playwright or an alternative; resolve before W2-β verification. |
| 2026-08-21 | **W1-B DONE** (Opus): three spec contracts committed; store-shopping-earns-cash exploit caught at spec time (STORE_OPEN gate). Wave 2 fully unblocked; W2-β + W2-γ launching. Ownership seam resolved: app/public/ belongs to W2-β, not W2-α. |
| 2026-08-21 | **W2-α DONE** (Go core: providers/engine/game/store/WS, -race clean, caught own data race) and **W2-β DONE** (frontend: full ui-spec DOM, store modal, dev harness; verified in real headless browser, caught 330px nested-coordinate bug). Committed e7504af. |
| 2026-08-21 | **W2-γ DONE** (45-sprite behind-view set + 37 thumbs, all assertions green, 3 visual bugs self-iterated, chair-region constraint caught 3 violations during authoring). Committed 40e712e. |
| 2026-08-21 | **W2-δ launched** (reconciliation): hoodie catalog fields, buddy_bot path, native /assets route replacing the symlink, catalog-vs-disk audit test. Next: overseer composed verification (server+fake provider+playwright), then W3. |
| 2026-08-21 | **W2-δ DONE** (seams reconciled + catalog-vs-disk test; committed b358d68). Design agent patched ui-spec §6 with the thumbForm/thumbDetail contract. |
| 2026-08-21 | **OVERSEER COMPOSED-VERIFICATION (Wave 2 gate): the game RUNS end-to-end** — real server + WS + sprites, screenshot judged by eye against the PDF mockups. Structure matches: behind-view scene, terminal scrolling compile lines, sprint panel, honest activity line, [S] STORE. **Gate FAILED on 3 defects**: (1) dev_form_* sprites are broken art — double-hump, center seam, NO hood dome; reads as two bald figures (the art agent's self-check composited with occluding props and missed it — its re-verification method now mandates a props-free silhouette test); (2) sprint units render the raw float ("38.19600152587…"); (3) [S] STORE button clips/wraps in the title bar. Art redo + frontend polish agents dispatched in parallel. This exact loop — run the real product, look at it — is what ADR 0011 bought us. |
| 2026-08-21 | Gate re-verification PASSED: dev sprite reads as one hooded figure (art redo e746f69); store modal = the PDF mockup realized; REAL buy->equip->see loop verified in the live browser (coins 125->5, zipper visible on the character's back). Frontend polish + robustness/diagnostics hardening landed. User's missing-scene report reproduced and made self-diagnosing (banner + /api/health). |
| 2026-08-21 | **Wave 3 begins**: Opus adversarial review of app/ (read-only). Remaining after review: fix wave via PR (restoring the PR ceremony trunk-work bypassed for velocity), README rewrite for the Go stack, mac field test by the user. |
| 2026-08-21 | **W3-A review verdict: FIX-FIRST.** BLOCKING: (B1) wildcard bind + InsecureSkipVerify origin = LAN/cross-site-readable activity monitor (probed from LAN+tailnet IPs and a cross-origin WS — accepted); (B2) STORE_OPEN gate released on reconnect and by any client's close (global flag, no refcount; frontend never reads state.storeOpen) — money-mint reproduced live at full typing rate. SHOULD-FIX: fake provider ships as the real provider on windows claiming HonestyGlobal; anti-mash constant declared 3x with no drift test and a ceiling that can't backstop; content-free structural test misses StateMessage/SaveData; schema written never read (newer saves silently downgraded); CI still builds only the frozen Rust. NITs incl. a uint64 overflow violating the migration invariant. Clean: privacy data model, economy arithmetic, server-side purchase validation, 24-client race stress. Fix wave dispatched on branch fix/v1-review-findings with PR ceremony. |
| 2026-08-21 | **PR #9 MERGED** (83f2561) — independent Opus merge-gate reproduced both blocker fixes live + clean-cache green. Final composed verification off merged main passed (health version 83f2561, scene+store correct). Tagging v1.0.0. Remaining: USER macOS field test (the only box no agent can tick). |
| 2026-08-21 | v1.0.0 shipped & user-confirmed working. User feedback: layout good, but procedural pixel art lacks the PDF's detail. Decision (user): pursue path A — push the generator with real pixel-art technique, keep 100% original/procedural. Phased: HERO pass (dev/monitor/room/chair) gated by overseer visual review BEFORE committing all 45. Post-v1 art track, does not touch shipped mechanics. |
| 2026-08-21 | User roadmap: (1) capture working process as project SKILLS, (2) analytics/activity-log feature — priced content-free signals (keystrokes/copy-paste/etc) -> coins + a workflow tracker, in autonomous phases, no human decisions. Wrote docs/plan/ROADMAP.md (analytics A1/A2/A3). Dispatching skills-author + analytics-A1 agents in parallel with the running art rollout (disjoint ownership: .claude/skills / app/ / tools+assets). |
| 2026-08-21 | User supplied the fidelity target (desired_fidelity.png) — isometric, rich-palette, hand-crafted. Overseer assessed it UNREACHABLE by the current procedural/18-colour/behind-view approach and surfaced the fork. **User decision: keep current look, ship features.** Art track PARKED (in-flight dithering rollout allowed to finish as style-consistency, not a chase). Effort -> analytics roadmap. 4 project skills landed (3eb0ea3). Parked art routes for later: user-generated sprites+pipeline / CC0 iso packs / bigger-palette iso generator. |
| 2026-08-21 | Analytics A1 shipped (19c8586) + art rollout committed & parked (189b1b8). New user roadmap: frontend architecture — TS + esbuild build pipeline, modular render/data/logic separation, Tauri desktop later, 'see how others do it'. Added Frontend Architecture track (F1/F2/F3) to ROADMAP. Sequencing call: do F1 (build+TS foundation) BEFORE analytics A2 to avoid rework. Dispatching F1. |
| 2026-08-21 | **F1 DONE (fb2761a)** — esbuild + TypeScript build pipeline under app/frontend/ (esbuild^0.24, typescript^5.6, strict tsconfig); game.js ported 1:1 to TS as a gated, behaviour-identical port; typed WS contract module (wire.ts) incl. A1 stats; build.mjs emits minified IIFE + sourcemap to app/public/js/. CI job: npm ci -> tsc --noEmit -> build -> git-diff drift check. Overseer-verified: tsc clean, deterministic rebuild, in-game parity render. (Loop had stalled after this commit before F2 was dispatched — restarted.) |
| 2026-08-21 | **F2 DONE** — modular separation of concerns. Split the monolithic main.ts into layers: utils (env/dom/format/assets/geometry), DATA/STATE (state/store.ts typed store + selectors, state/ws-client.ts owning connect/backoff/reconnect + a generic STORE_OPEN hold flag with zero modal knowledge), RENDER (render/{tint,scene,terminal,chrome,overlays,flash}, side-effect-free, never sends actions), FEATURE (features/{store-modal,activity-modal,keybindings}), dev/ harness, and a thin 61-line main.ts. No cross-layer reach-through (verified: render has no sendAction, ws-client imports no features). Overseer GATE PASSED in the real running game: scene renders (23 nodes), store+activity open/close via key+click+Esc, BUY 120 -> EQUIP -> ✓ EQUIPPED with cash 9999->9879 (exact -120), keystrokes tick live (438->468) under fake typing, zero console errors; tsc clean under strict (no `any`), byte-identical bundle across two builds. Next per sequencing: resume analytics A2. |
| 2026-08-22 | **A2 DONE** (analytics v1.2 — priced signals). Design-gated first (Opus): confirmed copy/paste chord is impossible permissionlessly on macOS (CGEventSource counts by event type, not keycode) → dropped to a future opt-in phase (ADR 0012); app-switch content-free on mac only → tracked/displayed but earns 0 so the economy is identical cross-platform; focus-session (sustained-typing block) is the clean new earning signal, both platforms. Key structural win: both shipped signals derive from data already on Snapshot (keystroke timing + ADR-0009 sanitized ActiveApp) → NO new provider observation, Snapshot + activity/content_free_test unchanged (that's the privacy proof). Implemented via parallel waves with exclusive ownership: GO-1 engine (focus tracker + FocusSessionBonusWork=2.0/120s + strategy test: deepFocus>steadyTypist>mouseOnly>masher=idle=0), GO-2 game (StatCounters+CoinBreakdown, proportional coin attribution at sprint payout via largest-remainder, conservation-proven, StoreOpen-gated, daily cap), GO-3 store (schema 2->3, additive, future-schema refusal preserved, migration round-trip test), TS-1 frontend (wire types + activity-modal breakdown, F2 boundaries respected), DOC-1 (ui-spec §6.1 + ADR index), + a polish pass (coins section: the "→" glyph was invisible in the pixel font → ASCII "->" + gold coin amount). Each Go wave independently gated by the overseer from a CLEAN test cache (-race). Full real-game gate PASSED: whole repo green, focus sessions tick live 0->1, coins breakdown renders + sums to payout (60=60), real schema-2 save migrated to 3 non-destructively, zero console errors, deterministic bundle, tsc clean. Next per roadmap: A3 (analytics over time). |
| 2026-08-22 | **A3 DONE** (analytics v1.3 — analytics over time). Design-gated first (Opus, ADR 0013): DROPPED hourly buckets (would reconstruct the user's daily schedule from counts — a privacy step down) in favor of "busiest DAY"; INCLUDED "longest focus block" as one content-free duration/day (the only engine touch). 30-day rolling history (sparse storage, honest gaps, never fabricated days), server-side streaks persisted independently of the window (so a streak can outlive 30 days), new [H] HISTORY modal with CSS block-bar charts (not canvas — the A2 pixel-font/AA lesson). Parallel waves, exclusive ownership: GO-0 engine (expose TickResult.FocusRunSeconds, additive, economy byte-identical), GO-1 game (history buckets + finalizeDay at rollover + the streak correctness core: active-day = activeSeconds>=300s, read-time effective streak folding today in, calendar-date arithmetic; 23 tests incl. all 8 §2.5 edge cases), GO-2 store (schema 3->4 additive, ErrFutureSchema preserved, finalize-on-reload-exactly-once, migration + round-trip tests), TS-1 frontend ([H] modal + wire types + dev fixture, F2 boundaries kept, self-gated), DOC-1 (ui-spec §6.1 + ADR index). Each Go wave independently gated from a CLEAN -race cache. Full composed real-game gate PASSED (overseer): whole repo green, bundle deterministic, tsc clean; crafted schema-4 save (6 history buckets + streak 5/9) loaded → server built the dense 30-day wire + streak → [H] modal rendered 7-day bars, 30-cell strip (7 active), busiest day 08/18 (240m), longest focus 80m, [H] via key+click+Esc, ZERO console errors; the read-time effective streak folded today in LIVE (seeded current=5, lastActive=yesterday → showed 6 once today's typing crossed threshold). User save restored byte-exact. Analytics track (A1/A2/A3) COMPLETE. |
| 2026-08-22 | **Art fix: dev now reads as SEATED** (was ambiguous — user flagged it looked "prone on a mat"). Root cause (overseer-diagnosed): CHAIR_RECT tops sat at/below DEV_RECT.top=92, so no chair back ever rose behind the dev's upper body → read as a flat mat. Confirmed the chair-SWAP itself works (earlier "swap does nothing" was a malformed devApply test passing a bare string instead of {itemId,tintId}). Fix (one Opus art agent, owned tools/gen_assets.py + assets/ + geometry.ts + rebuilt bundle): raised/enlarged all four CHAIR_RECTs so backs rise to/above the dev's head; redrew every chair with a tall backrest + armrests (basic mesh back+corner posts, racer stitched bolster wings, exec tufted high-back, antigrav rounded cradle pod — no longer an empty lump); refreshed 8 chair thumbnails. Kept palette/ramp discipline (tint headroom on ramp4, verified default slate not crushed), DEV_RECT stable. Overseer visual gate PASSED (own eyes, all four chairs + default): each unmistakably reads as seated; tsc clean, deterministic bundle, go build clean, zero console errors, save restored byte-exact. No sell/OSS/rename decisions made — user deliberating; this improvement lands regardless. |
| 2026-08-22 | **RENAMED dev-companion → dexel + went Apache-2.0 (OSS-ready, repo stays PRIVATE until user says).** Two parallel agents, disjoint ownership. CODE (app/** + CI): Go module `github.com/jawwadzafar/dev-companion/app` → `github.com/jawwadzafar/dexel/app` (22 files), save dir `~/.config/devcompanion/` → `~/.config/dexel/` (CLEAN rename, NO migration per user — nothing shipped, start fresh), env `DEVCOMPANION_*` → `DEXEL_*` (no back-compat), brand strings (titlebar, <title>, log line, package.json) → dexel; in-game flavor ("Dev Cash", "DEV STORE", "dev" character) preserved. Left legacy.go's LegacyPath() on "dev-companion" deliberately — it's the frozen Rust binary's REAL save path, not our brand. DOCS (README/docs/AGENTS + LICENSE): brand rename (25 edits/10 files), added LICENSE (Apache-2.0, Copyright 2026 Jawwad Zafar), NOTICE, THIRD-PARTY-LICENSES.md (NES.css MIT, Press Start 2P OFL-1.1, nhooyr websocket ISC, esbuild MIT, TypeScript Apache-2.0); left frozen-Rust paths / historical logs / real external refs unchanged (would falsify records). Overseer gate PASSED: clean-cache `go build`/`go vet`/`go test -race` all green on the dexel module path; tsc clean; deterministic bundle; real-run shows "dexel listening" + <title>dexel; no brand stragglers outside intentional legacy exceptions. |
| 2026-08-22 | **UI-1 DONE — hamburger menu + coin top-left.** Replaced the three separate [A]/[H]/[S] titlebar launchers with one ☰ hamburger (top-right, built from 3 CSS bars not a glyph — A2 lesson) opening a dropdown listing Store/Activity/History; moved Dev Cash/coin + LV + wordmark into a top-left cluster. New feature module features/menu.ts (open/close/toggle, outside-click + Esc close, generic .menu-item routing so future sections need zero code here); the store/activity/history modal modules + their ids untouched (menu just relocates the launchers). Keybindings: M toggles menu, S/A/H still open modals directly (closing the menu first). Agent caught a real bug via click-testing: a `position:static` override broke NES.css .nes-btn's ::after pixel-shadow containing block, ballooning pseudo-elements so every menu click hit History — fixed. Overseer gate PASSED (own eyes + interaction): coin at x184 (left), hamburger at x600 (right), menu opens, all three sections open from it + menu closes, keyboard S/A/H still work, dexel wordmark + fixed seated chair visible, zero console errors; tsc clean, deterministic bundle, go build clean. |
| 2026-08-22 | **UI-2 DONE — README screenshots refreshed + cross-platform build docs.** Overseer re-captured hero/store/history from the real running game (now showing the dexel titlebar, the hamburger UI + top-left coin, and the fixed seated chair — incl. the chair in the store preview pane) → docs/images/. Docs agent added a "Building from source" section: prerequisites (Go 1.27+; Node only to change frontend), `go build -o dexel .` per-OS, a platform activity-capture matrix (macOS permissionless / Linux input-group / Windows+other = blind, no native provider yet, honest per ADR 0010), cross-compile notes, and a "packaged desktop app via Tauri (planned)" pointer. Verified: links/images resolve, flags match main.go, provider behavior accurate. |
| 2026-08-22 | **Night pipeline status + F3 buildability finding.** Committed: SEC-1 design (ADR 0014) + F3 design (ADR 0015). RUNNING: UI-fix (BUG-1/2/3, frontend bundle), SEC-1 GO-1 (internal/store integrity+config). QUEUED sequentially: art-fix BUG-4/5/6/7 (after UI-fix — shares the bundle), SEC-1 GO-2 (main.go, after GO-1), F3-T2 (main.go ephemeral-port + stdout handshake + -allow-origin, after SEC-1 GO-2 since same file). **F3 CONSTRAINT:** this build environment has NO Rust toolchain and NO webkit2gtk, so a Tauri bundle CANNOT be built/gated here. Decision: do the gateable Go-side prep (T2) tonight; DEFER the Tauri scaffold (T1)/sidecar script (T3)/icons (T4)/CI (T5) + the actual bundle build to an env with Rust+webkit — per ADR 0015's phasing this is the owner registering their Mac (and later a Windows box) as self-hosted runners. Not writing unverified Tauri config as if it works. Owner decisions for the morning: (1) F3 platform/runner plan (register your Mac for the .dmg; Windows later); (2) SEC-1 baked-key vs OS-keychain (design recommends baked key). |
| 2026-08-22 | **BUG-1/2/3 FIXED (UI).** BUG-1: hamburger menu now has a "MENU" title line above Store/Activity/History. BUG-2: top-left is coin-then-level only; removed the dexel wordmark + mood dot (chrome.ts guarded so nothing throws; mood still shown in the status panel). BUG-3: fixed vertically-clipped text (8px→10px line-height on #sprint-name/#status-line/#ticker/.card .name/preview — they lacked an explicit line-height; "SPRINT" now reads correctly, was "SPRTNT"); deliberately LEFT #terminal at 8px (verified its glyphs weren't clipped and that growing it spills text past the monitor.png's 88px screen region). Found + logged BUG-8 (activity modal 396px exceeds the dialog max-height cap at 400px viewport → footer clipped; pre-existing). Overseer gate PASSED (own eyes): coin@x8 before LV@x80, no wordmark/mood, MENU title, SPRINT text fully visible, zero console errors; tsc clean, deterministic bundle. |
| 2026-08-22 | **SEC-1 DONE — save integrity / anti-cheat.** GO-1 (internal/store): integrity.go (baked HMAC-SHA256 key, openly documented; MAC over the whole SaveData struct with Mac zeroed; float unitsDone quantized to 6dp for MAC stability), config.go (unsigned config.json {name}), SaveData += Mac, schema 4→5 (grandfather schema-4 trusted then re-sign), ErrTampered (renames bad file to state.json.invalid, distinct from not-found), 15 new tests incl. the anti-cheat vectors. GO-2 (main.go): loadOrImport now returns early on ErrTampered/ErrFutureSchema so a tampered save can NEVER reach legacy-import (the re-grant hole); config load/init independent of state. Overseer end-to-end gate PASSED (own run): signed save (devCash 65 + mac) → hand-edit devCash to 999999 → restart logs "save integrity check failed; starting a fresh economy", quarantines the cheat to state.json.invalid (999999 preserved there, NEVER loaded), boots at baseline; genuine saves load fine; config.json name editable without tripping integrity. Honest ceiling documented: stops casual JSON editing, not a source-reader (accepted for an OSS local game). Whole repo green -race, clean cache. In-game name UI deferred (config plumbing in place). |
| 2026-08-22 | **F3-T2 DONE — Go-side Tauri prep (main.go).** Explicit net.Listener before serve so `-addr 127.0.0.1:0` binds an OS-assigned free port; a stdout handshake `DEXEL_LISTENING http://127.0.0.1:<port>` reports the resolved address (for the future Tauri shell to read then point its webview); a new `-allow-origin` flag appends LITERAL WS origins only (bare host or origin URL; wildcard rejected fatally — never a second -insecure-origin), for a future embedded webview. Default behavior byte-equivalent (loopback bind, same origin patterns from the resolved port); security unchanged. Gated clean-cache (no server run — art agent was using :8080): go build/vet green, `go test ./... -race` green incl. new main_test.go (ephemeral-port/handshake/allow-origin/wildcard-reject table tests); agent's own smoke test showed real ephemeral port + curl health + WS 101 for tauri://localhost / 403 for foreign / fatal on `*`. Tauri scaffold (T1/T3/T4/T5) remains DEFERRED — no Rust/webkit build env here (needs the owner's Mac/Windows runners per ADR 0015). |
| 2026-08-22 | **BUG-4/5/6/7 FIXED (art, attempt #2).** Root cause the first pass missed: the head's top sat ABOVE the keyboard hard-region line (room y115), so no chair could ever paint over it — the fix was LOWERING the head, not raising chairs. One coordinated redraw (tools/gen_assets.py; geometry.ts + scene.ts unchanged): hood dome apex dropped room y115→y126 (now below the chair-back top at y120/121); arms pulled outward + hood narrowed → a 4px transparent gap each side so hood + 2 arms read as three distinct forms (BUG-6); all four chair backs painted full-width to the hard-region top with always-visible DETAIL-layer markers (basic mesh grid, racer cream stitching, exec gold tufting, antigrav teal glow rim) so the chair top reads above the head at ANY tint and stays clearly distinct in value/hue from the purple hoodie (BUG-4, BUG-5); hooded behind-view drape per reference (BUG-7). Hoodie overlays + thumbs regenerated to match the new pose. Overseer visual gate PASSED (own eyes, basic/exec/antigrav + idle): chair top now sits above the nestled hood, hoodie distinct from chair, arms separate from hood, default slate not crushed. gen_assets self-checks pass, deterministic assets+bundle, tsc/go build clean, save restored byte-exact. |
| 2026-08-22 | **BUG-8 FIXED — Activity modal no longer clipped.** #activity was 396px but a native dialog:modal is UA-capped at 362px @400px viewport, silently clamping + scroll-clipping the footer and last coins row. Fixed (game.css #activity only): height 396→346px, re-centered top 2→27px, .activity-row 16→12px (still ≥10px BUG-3 safe minimum), sections repositioned. CSS-only (bundle unchanged). Overseer gate PASSED (own eyes): full modal renders — TODAY(7)+LIFETIME(7)+COINS(4)+footer "A / ESC CLOSE" all visible, no clip, zero console errors; store/history modals unaffected. |
| 2026-08-22 | **NIGHT PIPELINE COMPLETE — cumulative verification PASSED.** All filed work shipped + gated + pushed (repo stays PRIVATE): rename→dexel + Apache-2.0 (LICENSE/NOTICE/THIRD-PARTY); PRODUCT-EVOLUTION.md; hamburger UI; README screenshots + cross-platform build docs; SEC-1 anti-cheat (design ADR 0014 + impl, tamper→reset verified end-to-end); F3 design (ADR 0015) + F3-T2 Go prep (ephemeral port/handshake/-allow-origin); BUG-1/2/3 (UI: menu title, coin-first top-left, un-clipped text); BUG-4/5/6/7 (art: seated read v2 — head lowered so chair-top reads above it, hoodie/chair distinct, hood/arms separated); BUG-8 (activity modal fits). Final cumulative gate: clean-cache `go build`/`go vet`/`go test ./... -race` all green on the dexel module; frontend tsc clean; committed bundle matches fresh build (no CI drift); composed real-game smoke — hamburger→Store/Activity/History all open, zero console errors. DEFERRED (needs owner): F3 Tauri bundle (no Rust/webkit build env here → owner's Mac/Windows self-hosted runners per ADR 0015). AWAITING OWNER greenlight (not started autonomously): PRODUCT-EVOLUTION phases P1 Identity / P2 Sessions / … Overseer holding rather than building unreviewed product features. |
