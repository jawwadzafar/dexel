# dev-companion — PR Log

One row per milestone PR, appended by pr-merge-decider after every decision.
Never delete or rewrite a past row — append corrections as new rows.

| PR | Milestone | correctness | boundaries | tests | Decision | Merge SHA |
|----|-----------|-------------|------------|-------|----------|-----------|
| #1 | M0 | Approve | Approve (no veto) | Approve | Merged | 84895215f5958d15612fb1c6ac34b1a8815dce2c |
| #2 | M1 | not run | not run | not run | **Merged without review** — explicit user request, opencode offline, no reviewer subagents available | 4cda6b5 |
| #2 | M1 | Approve (backfill) | Approve (backfill, no veto) | Approve (backfill) | **Backfilled review** — audit record only; PR already merged. All 3 reviewers independently re-verified commit 4cda6b5 in isolated worktrees; all approve. No defects requiring fix-forward. | 4cda6b5 |
| #3 | M2 | pending | pending | pending | **Opened by game-engineer** (this row will be updated by pr-merge-decider when the 3 reviewers finish) | — |
| #3 | M2 | Approve | Approve (no veto) | Approve | **Merged** — 3/3 reviewers Approve (correctness, boundaries no-veto, tests); landed on main via SSH fast-forward push (commit 0f45267) because the gh CLI token was invalid (401) and could not re-authenticate non-interactively. PR #3 left open on GitHub (could not mark MERGED/close via gh). | 0f45267 |
| #4 | M3 | Approve | Approve (no veto) | Approve | **Merged** — 3/3 reviewers Approve (correctness: fmt/clippy clean, 24/24 tests, plan §3.2 scope, no M4/M5 leak; boundaries: all 3 non-negotiables clean, no veto; tests: 24/24 green ×2 independent runs, no skips). Reviews taken at PR head `6cc0910`; one harness-only commit `7413a5e` (DeepSeek-down model-tier fix + .gitignore, zero game-code diff) landed on the branch after review and rides along in the squash. `gh pr review --approve` refused (same account as PR author — GitHub blocks self-approval), so the decider's approval was recorded in the handoff only. | 255c789cc00149f7d7448b00c87611fb4005d2ff |
| #5 | M4 | Approve | Approve (no veto) | Approve | **Merged** — 3/3 reviewers Approve (correctness: plan §3.4 shape verbatim, exit criterion re-verified in own worktree + live `cargo run` smoke, 33/33, fmt/clippy clean; boundaries: activity crate byte-identical to main, no raw content serialized, anti-mash clamp intact, no veto; tests: 33/33 ×2 independent runs, 9 new M4 tests non-vacuous, 3× re-runs stable, no skips, no shared data-dir). Merged 2026-08-20 via `gh pr merge 5 --squash --delete-branch` (gh auth green, no fallback needed); PR state MERGED, remote branch deleted. `gh pr review --approve` not posted (same account as PR author — self-approval blocked, same as M3); approval recorded in handoffs only. | 4bc55a1a90a185bb211eb3e9e47debd95922ce4b |
 | #6 | M5 | Approve | Approve (no veto) | Approve | **Merged — M5 completes v0.1.** 3/3 reviewers Approve at head `10e830ca` (correctness: all four §4/M5 items re-derived in own worktree — debug counter fully removed, desk upgrade verified in code + live run (wallet-120 seeded save → `desk upgrade unlocked`), mood line matches plan example, release binary launched outside `cargo run` (restore + unlock + 30 s autosave, no panic/B0001), 38/38, fmt/clippy clean, scope 3 files only; boundaries: all 3 non-negotiables PASS with NO veto — `activity/` byte-identical to main & Bevy-free, SaveData unchanged (desk upgrade derived from Wallet, non-persistent), anti-mash clamp + `progress_delta` untouched, Bevy 0.19 `Visibility`-component toggle judged within plan shape; tests: 38/38 ×2 independent runs + 5× app-driven re-runs, no flakes/skips, both M4-gap tests exercise their named branches, 2 non-blocking coverage gaps noted: re-hide-after-unlock case, `mood_render_system` ParamSet wiring only via the pure `mood_line` fn). Merged 2026-08-20 via `gh pr merge 6 --squash --delete-branch` (gh auth green, no fallback); PR state MERGED, remote branch deleted. `gh pr review --approve` not posted (same account as PR author — self-approval blocked, same as M3/M4); approvals recorded in handoffs only. Standing carry-over: human visual smoke test on a live display (dead X display, same note M2–M5). | f38901bdeeee3936fa4f1be017b973ad4880b0b1 |
| #7 | Bugfix (HUD not rendering, human report handoff #10) | Approve | Approve (no veto) | Approve | **Merged** — 3/3 code reviewers Approve (correctness: split mechanical — only `fn main`→`pub fn run`, fix parents HUD under desk root via `ChildSpawnerCommands` exactly as the bug report prescribes, regression test `hud_is_a_child_of_the_desk_root` re-derived to FAIL on pre-fix code, 39/39, fmt/clippy clean, scope 3 files; boundaries: `activity/` byte-identical to main & Bevy-free, SaveData untouched, anti-mash clamp untouched, bug-contract scope respected, no veto; tests: 39/39 ×2 from clean worktree, new test non-flaky 5/5, revert-check proves it fails pre-fix & passes on fix, no regression base 38→39). **Visual verification: BLOCKED (environmental — no display in-session: `DISPLAY` empty, `xdpyinfo` fails on :0/:1/:99/:1025, no Xvfb/Xephyr/weston, no screenshot tool, Xorg console-restricted, no sudo)** — per handoff #10's clause BLOCKED does not block merge but is a carried gap, never upgraded to verified on the strength of tests. Merged 2026-08-20 via `gh pr merge 7 --squash --delete-branch` (gh auth green, no fallback); PR state MERGED, remote branch deleted. `gh pr review --approve` not posted (same account as PR author — self-approval blocked, same as M3/M4/M5); approvals recorded in handoffs + this log. **Carried gap (visual-verification of PR #7, owned by human):** on a live display, launch `cargo run -p companion` and run `python3 scripts/visual-check.py <screenshot> "Describe every UI element you can see, including any progress bar and any numeric counters (coins, level, XP). Is there a bottom bar with text? Is there a desk area with colored rectangles above it?"` — the description must independently name a **progress bar** AND ≥1 **numeric readout** (coins or level) with the desk rects still visible; seed `~/.local/share/dev-companion/save.json` (e.g. wallet 65, xp 40, project index 2, work_done 36.9) to make readouts/bar unambiguous (exact question + seeded save in `11-game-engineer-to-visual-verifier.md`). | 1de4483f410df903f489b7718c0d3f08208ac38d |
