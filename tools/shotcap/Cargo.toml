[package]
name = "shotcap"
version.workspace = true
edition.workspace = true
license.workspace = true

# Development/verification tool: runs the real game scene in-process with a
# seeded save and captures its own framebuffer to PNG via Bevy's Screenshot
# component. Not part of the shipped game — see README "Visual verification".
[dependencies]
companion = { path = "../../companion" }
bevy = "0.19.1"
